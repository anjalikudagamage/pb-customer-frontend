import { SxProps, Theme } from '@mui/system';

export const imageListStyles: SxProps<Theme> = {
  width: 1200,
  height: 750,
};

export const imageListItemStyles: SxProps<Theme> = {
  // Add specific styles for ImageListItem here if needed
};

export const imageStyles: React.CSSProperties = {
  // Add specific styles for img element here if needed
};
